# Hands-On 5.1.2: Class Components (`StudentApp`)

- **Objective:** Learn to create and use React Class Components.
- **Implementation:** Created a `Student` component that extends `React.Component`. It uses the `render()` method to return JSX and accesses properties via `this.props`. Instantiated it multiple times inside `App.js`.
- **Verification:** App successfully builds and runs.
