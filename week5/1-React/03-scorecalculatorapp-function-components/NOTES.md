# Hands-On 5.1.3: Function Components (`scorecalculatorapp`)

- **Objective:** Learn to create and use React Function Components.
- **Implementation:** Created a `ScoreCalculator` component using modern ES6 arrow function syntax. It takes props (`math`, `science`, `english`), performs basic computations (total and average), and returns JSX. Rendered instances in `App.js`.
- **Verification:** App successfully builds and runs.
