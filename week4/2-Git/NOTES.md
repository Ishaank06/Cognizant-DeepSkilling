# Hands-On 4.2: Git Workflows (1 to 5)

- **Ex 1 — Git Setup & First Commit:** Initialized a local `GitDemo` repository, configured the local user, created `welcome.txt`, and executed the first commit.
- **Ex 2 — .gitignore:** Created a `.gitignore` file to successfully exclude `*.log` files and the `log/` directory from version control.
- **Ex 3 — Branching & Merging:** Created `GitNewBranch`, committed new files, and performed a fast-forward/clean merge back into `master`.
- **Ex 4 — Conflict Resolution:** Intentionally created a merge conflict by editing the same line in `welcome.txt` on both `master` and `GitNewBranch`. Resolved the conflict by manually editing the file and finalizing the merge commit.
- **Ex 5 — Cleanup:** Deleted `GitNewBranch` after the successful merge.
